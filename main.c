#include <stdio.h>
#include <stdlib.h>

int main()
{
    int base;
    int altura;
    printf("Ingrese la base: %d", base);
    scanf("%d",&base);
    printf("Ingrese la altura: %d", altura);
    fflush(stdin);
    scanf("%d", &altura);

    return 0;
}
